# my-custom-app
This a simple python and flask app used to learn building docker images and running docker containers.
The Dockerfile is used to install all the dependecies that enable the application to run successfully.\
It also shows how the application code is copied inside the image and the command executed as soon as the\
docker image is ran.
The app.py file contains the application file which is written in python language.
--END
